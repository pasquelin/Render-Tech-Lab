# Protocol

Use the public runner exported by `index.ts`. The existing [README](../README.md) records the runnable commands and experiment protocol. Commands and imports use canonical paths; no compatibility files remain. No new campaign is produced by the structural tests.
