# Bench Report: 02-gpu-frustum-culling

> **Statut : pas encore implémenté — aucune mesure.**

Ce module ne contient aucun code : ni `baseline/`, ni `implementation/`, ni `benchmark/`.
Aucune campagne n'a donc jamais été exécutée.

La version précédente de ce fichier annonçait une décision contractuelle `INTEGRATE`
et une réduction de 92,7 % du temps de soumission CPU. Ces valeurs n'ont été produites
par aucun harnais : elles ont été retirées.

Le `latest.json` correspondant a été retiré pour la même raison.
