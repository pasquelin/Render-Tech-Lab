import { Button } from './Button.tsx';
export function SegmentedControl<T extends string>({value,options,onChange,disabled=false,label}:{value:T;options:{value:T;label:string;id?:string}[];onChange:(value:T)=>void;disabled?:boolean;label:string}){return <div role="group" aria-label={label} className="join w-full bg-base-100 p-0.5 rounded-box border border-base-content/10">{options.map(option=><Button id={option.id} key={option.value} variant={option.value===value?'primary':'ghost'} className="join-item flex-1" disabled={disabled} aria-pressed={option.value===value} onClick={()=>onChange(option.value)}>{option.label}</Button>)}</div>;}
