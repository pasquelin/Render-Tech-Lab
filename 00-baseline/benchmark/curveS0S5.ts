// The previous Node-only curve used fabricated timings. Physical samples are required.
console.error('not-run: S0–S5 requires a browser GPU campaign; no result has been written.');
process.exitCode = 1;
export {};
